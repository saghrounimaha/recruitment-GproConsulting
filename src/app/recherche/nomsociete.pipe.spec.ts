import { NomsocietePipe } from './nomsociete.pipe';

describe('NomsocietePipe', () => {
  it('create an instance', () => {
    const pipe = new NomsocietePipe();
    expect(pipe).toBeTruthy();
  });
});
